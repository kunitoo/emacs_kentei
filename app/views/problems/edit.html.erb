<%- model_class = @problem.class -%>
<h1><%=t '.title', :default => t('helpers.titles.edit', :name => @problem.title) %></h1>
<ul class="unstyled">
  <li><i class="icon-plus"></i> 問題の意味を変更するような修正はおやめください。</li>
  <li><i class="icon-plus"></i> 正解選択肢は変更できません。</li>
</ul>

<%= render :partial => 'form', locals: { edit: true } %>
